%h1 Create a new article

// Print validation errors if any
- if @article.errors.any?
  %p.error
    %strong The following errors prevented the article from being saved:
    %ul
      - @article.errors.full_messages.each do |msg|
        %li
          = msg

= form_with scope: :article, url: articles_path, method: :post do |f|
  %p 
    = f.label :title 
    %br/
    = f.text_field :title
  %p
    = f.label :description 
    %br/
    = f.text_area :description

  = f.submit "Create Article"