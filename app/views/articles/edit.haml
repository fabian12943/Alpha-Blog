%h1 Edit article

%a(href="/articles") Return to articles listing

// Print validation errors if any
- if @article.errors.any?
  %p.error
    %strong The following errors prevented the article from being updated:
    %ul
      - @article.errors.full_messages.each do |msg|
        %li
          = msg

= form_with(model: @article, local: true) do |f|
  %p 
    = f.label :title 
    %br/
    = f.text_field :title
  %p
    = f.label :description 
    %br/
    = f.text_area :description
  = f.submit "Update Article"