- if flash.any?
  .container
    - flash.each do |type, msg|
      %div{class: "alert #{flash_class(type).to_s} alert-dismissible fade show mt-4", role: "alert"}
        %strong
          = msg
        %button.btn-close{"aria-label" => "Close", "data-bs-dismiss" => "alert", :type => "button"}